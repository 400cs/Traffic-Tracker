import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.BenNguyen.traffictracker',
  appName: 'Traffic Tracker',
  webDir: 'www'
};

export default config;
